#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main ()
{
   int Eight = pow(2,3);
   int Nine = ceil(8.9);
   int Ten = floor(10.1);

   //remainder: 
   int One = 22 % 3;
   printf("%d", One);
}