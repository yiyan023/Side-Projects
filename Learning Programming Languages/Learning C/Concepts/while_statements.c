#include <stdio.h>
#include <stdlib.h>

int main () {
    int index = 0;
    while (index <= 5) {
        printf("%d\n", index);
        index ++ ;
    }

    /* do-while loops also exist, but are not as common */
}